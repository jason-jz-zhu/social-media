Anthropic just leaked their next model.

My first reaction wasn't excitement.

↓ It was a question ↓

"Claude Mythos" — described as a "step change" in capabilities.

Currently testing with early access customers.

I spend $450/month on Claude. I've built harness architectures around it. I've mapped its failure modes.

So when I hear "step change," I don't think "cool, better AI."

I think: which parts of my workflow just became obsolete?

Here's what "step change" has meant historically:

GPT-3 → GPT-4: enabled new task categories entirely
Claude Opus 4.5 → 4.6: eliminated the need for sprint decomposition in my harnesses

Every harness component encodes an assumption about model limitations.

When the model improves → re-audit the harness.

Last week I tested 4 architectures around the same model.

Result: harness design matters more than model choice.

But step changes shift the boundary.

→ Old guardrails become dead weight
→ New, more ambitious architectures become possible
→ What used to require 3 agents might need 1
→ What was impossible with any architecture becomes a solo task

When Mythos drops, here's my plan:

Re-run the same experiment. Same task. New model. See which harness components are still load-bearing — and which ones the model has outgrown.

Model upgrades don't make harness design obsolete.

They make it more interesting.

What's your first move when a new model drops — test old workflows, or try something completely new?

#AI #HarnessDesign #ClaudeMythos
